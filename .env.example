# ============================================
# Stellux - Docker Environment Config
# ============================================
# Copy this file to .env and update the values
#   cp .env.example .env

# -----------------
# 必须修改的配置
# -----------------

# 数据库密码（生产环境必须修改）
POSTGRES_PASSWORD=your-secure-password-here

# JWT 密钥（至少32个字符，用于用户认证）
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters

# RustFS 存储密码
RUSTFS_SECRET_KEY=your-secure-rustfs-secret-key

# -----------------
# 前端 API 地址
# -----------------
# 通过 Next.js 代理转发到后端，使用相对路径即可
# 前端 3000 端口会自动代理 /api/v1/* 到后端 8080
NEXT_PUBLIC_API_URL=/api/v1

# -----------------
# 管理员账号
# -----------------
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# -----------------
# 可选配置（一般不需要修改）
# -----------------

# 数据库
POSTGRES_DB=stellux
POSTGRES_USER=stellux
POSTGRES_PORT=5432

# 后端端口
BACKEND_PORT=8080

# 前端端口
FRONTEND_PORT=3000

# RustFS
RUSTFS_ACCESS_KEY=stelluxadmin

# JWT
JWT_ACCESS_TOKEN_EXPIRE_HOURS=720
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30

# 日志级别 (debug, info, warn, error)
RUST_LOG=warn

# 数据库连接池
DATABASE_MAX_CONNECTIONS=25
DATABASE_MIN_CONNECTIONS=3
DATABASE_ACQUIRE_TIMEOUT_SECS=10
DATABASE_IDLE_TIMEOUT_SECS=600
DATABASE_MAX_LIFETIME_SECS=1800

# 服务器
SERVER_MAX_BODY_SIZE_MB=50

# -----------------
# Docker 预构建镜像
# -----------------
# BACKEND_IMAGE=sexyfeifan/stellux-backend:latest
# FRONTEND_IMAGE=sexyfeifan/stellux-frontend:latest
